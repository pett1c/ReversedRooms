using Reversedrooms.Models.Items;

namespace Reversedrooms.Models
{
    public class Note : Item
    {
        public Note(string name, string description) : base(name, description)
        {
        }
    }
}